{
  "name": "escape-run",
  "version": "1.0.0",
  "description": "A free, open-source, privacy-first math game for kids age 6–8. Zero runtime dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test",
    "check": "for f in $(find src -name '*.js'); do node --check \"$f\" || exit 1; done",
    "gen-audio": "node tools/gen-audio.mjs"
  },
  "keywords": ["kids", "education", "math", "game", "pwa", "offline", "canvas"],
  "license": "MIT"
}
